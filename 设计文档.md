# 学校洗衣机清洗服务系统 - 设计文档

## 1. 项目概述

学校洗衣机清洗服务系统，为在校学生提供洗衣机上门清洗服务。系统包含三个端：**用户端（H5）**、**管理端（H5）**、**员工端（H5）**，全部适配手机浏览器访问。

### 核心业务逻辑

用户下单 → 填写楼栋/房间号/上门时间 → 生成未支付订单 → 展示管理员微信二维码 → 用户扫码加微信转账 → 管理员后台确认已支付 → 员工端收到通知 → 员工抢单 → 上门服务 → 完成

---

## 2. 技术栈

| 层次 | 技术 | 说明 |
|------|------|------|
| 后端框架 | Spring Boot 2.7+ | Java 后端 |
| ORM | MyBatis-Plus | 数据库操作 |
| 数据库 | MySQL 8.0 | 数据存储 |
| 缓存 | Redis | 抢单锁、会话管理 |
| 实时通信 | WebSocket | 员工端订单通知 |
| 前端框架 | Vue 3 + Vant 4 | H5移动端UI |
| 构建工具 | Vite | 前端打包 |
| 接口文档 | Knife4j / Swagger | API文档 |

---

## 3. 系统架构图

```
┌─────────────────────────────────────────────────────────┐
│                      前端 H5                            │
│  ┌───────────┐  ┌───────────┐  ┌───────────┐          │
│  │ 用户端     │  │ 管理端     │  │ 员工端     │          │
│  │ /user/*   │  │ /admin/*  │  │ /emp/*    │          │
│  └─────┬─────┘  └─────┬─────┘  └─────┬─────┘          │
│        │              │              │                  │
└────────┼──────────────┼──────────────┼──────────────────┘
         │              │              │
         ▼              ▼              ▼
┌─────────────────────────────────────────────────────────┐
│                Nginx（可选，路由分发）                    │
└─────────────────────────┬───────────────────────────────┘
                          │
┌─────────────────────────▼───────────────────────────────┐
│              后端 API（Spring Boot）                     │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐  │
│  │用户Controller│管理Controller│员工Controller│WebSocket │  │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘  │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐               │
│  │  Service │ │  Mapper  │ │  Entity  │               │
│  └──────────┘ └──────────┘ └──────────┘               │
└─────────────────────────┬───────────────────────────────┘
                          │
              ┌───────────┴───────────┐
              ▼                       ▼
        ┌──────────┐           ┌──────────┐
        │  MySQL   │           │  Redis   │
        └──────────┘           └──────────┘
```

---

## 4. 数据库设计

### 4.1 用户表（user）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键ID |
| openid | VARCHAR(64) | 微信openid（可选） |
| nickname | VARCHAR(50) | 昵称 |
| phone | VARCHAR(20) | 手机号 |
| create_time | DATETIME | 创建时间 |

### 4.2 管理员表（admin）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键ID |
| username | VARCHAR(50) | 登录账号 |
| password | VARCHAR(255) | 登录密码（加密） |
| name | VARCHAR(50) | 管理员姓名 |
| wechat_qrcode | VARCHAR(255) | 微信收款二维码图片URL |
| create_time | DATETIME | 创建时间 |

### 4.3 员工表（employee）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键ID |
| username | VARCHAR(50) | 登录账号 |
| password | VARCHAR(255) | 登录密码（加密） |
| name | VARCHAR(50) | 员工姓名 |
| phone | VARCHAR(20) | 手机号 |
| status | TINYINT | 状态：0-空闲, 1-服务中 |
| create_time | DATETIME | 创建时间 |

### 4.4 订单表（orders）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键ID |
| order_no | VARCHAR(32) | 订单编号 |
| user_id | BIGINT | 用户ID |
| building | VARCHAR(50) | 楼栋 |
| room_no | VARCHAR(50) | 房间号 |
| service_time | DATETIME | 预约上门清洗时间 |
| status | TINYINT | 状态：0-未支付, 1-已支付待服务, 2-服务中, 3-已完成, 4-已取消 |
| employee_id | BIGINT | 接单员工ID（可为空） |
| remark | VARCHAR(255) | 备注 |
| create_time | DATETIME | 创建时间 |
| pay_time | DATETIME | 支付确认时间 |
| complete_time | DATETIME | 完成时间 |

### 4.5 订单状态日志表（order_status_log）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键ID |
| order_id | BIGINT | 订单ID |
| from_status | TINYINT | 原状态 |
| to_status | TINYINT | 新状态 |
| operator_type | TINYINT | 操作者类型：0-用户, 1-管理员, 2-员工 |
| operator_id | BIGINT | 操作者ID |
| create_time | DATETIME | 创建时间 |

---

## 5. 订单状态机

```
                    ┌──────────┐
                    │  UNPAID  │  (0-未支付)
                    └────┬─────┘
                         │ 管理员确认支付
                    ┌────▼─────┐
                    │   PAID   │  (1-已支付待服务)
                    └────┬─────┘
                         │ 员工抢单
                    ┌────▼────────┐
                    │ IN_PROGRESS │  (2-服务中)
                    └────┬────────┘
                         │ 员工完成服务
                    ┌────▼──────┐
                    │ COMPLETED │  (3-已完成)
                    └───────────┘

  任何状态下可取消 ──→ CANCELLED (4-已取消)
```

---

## 6. API 接口设计

### 6.1 用户端接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/user/login | 用户登录（微信或手机号） |
| POST | /api/user/order/create | 创建订单 |
| GET | /api/user/order/list | 我的订单列表 |
| GET | /api/user/order/detail/{id} | 订单详情 |
| POST | /api/user/order/cancel/{id} | 取消订单 |
| GET | /api/user/admin/qrcode | 获取管理员微信二维码 |

### 6.2 管理端接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/admin/login | 管理员登录 |
| GET | /api/admin/order/list | 订单列表（可筛选状态） |
| GET | /api/admin/order/detail/{id} | 订单详情 |
| POST | /api/admin/order/confirm-pay/{id} | 确认已支付 |
| POST | /api/admin/employee/add | 添加员工 |
| POST | /api/admin/employee/remove/{id} | 删除员工 |
| GET | /api/admin/employee/list | 员工列表 |
| GET | /api/admin/statistics | 数据统计 |

### 6.3 员工端接口

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/employee/login | 员工登录 |
| GET | /api/employee/order/available | 可抢单列表 |
| POST | /api/employee/order/grab/{id} | 抢单 |
| GET | /api/employee/order/my-list | 我的订单 |
| POST | /api/employee/order/start/{id} | 开始服务 |
| POST | /api/employee/order/complete/{id} | 完成服务 |

### 6.4 WebSocket 接口

| 端点 | 说明 |
|------|------|
| ws://host/ws/employee/{token} | 员工端WebSocket连接，新订单推送 |

---

## 7. 前端页面设计

### 7.1 用户端页面

| 页面 | 路由 | 说明 |
|------|------|------|
| 首页 | /user/home | 展示服务介绍、下单入口 |
| 下单页 | /user/order/create | 表单：楼栋+房间号+时间+备注 |
| 支付页 | /user/order/pay/{id} | 展示管理员微信二维码+订单信息 |
| 订单列表 | /user/order/list | 我的所有订单 |
| 订单详情 | /user/order/detail/{id} | 订单状态追踪 |

### 7.2 管理端页面

| 页面 | 路由 | 说明 |
|------|------|------|
| 登录页 | /admin/login | 账号密码登录 |
| 订单管理 | /admin/order/list | 全部订单，按状态Tab筛选 |
| 订单详情 | /admin/order/detail/{id} | 订单信息 + "确认已支付"按钮 |
| 员工管理 | /admin/employee/list | 员工列表，增删员工 |
| 数据统计 | /admin/statistics | 订单量、收入概览 |

### 7.3 员工端页面

| 页面 | 路由 | 说明 |
|------|------|------|
| 登录页 | /employee/login | 账号密码登录 |
| 待抢单 | /employee/order/available | 可抢订单列表，实时刷新 |
| 我的订单 | /employee/order/my-list | 已接订单 |
| 订单详情 | /employee/order/detail/{id} | 订单信息 + 开始/完成操作 |

---

## 8. 关键功能详细设计

### 8.1 抢单逻辑（防并发）

```
1. 员工点击"抢单"
2. 后端用 Redis 分布式锁：SETNX order:lock:{orderId} {employeeId}
3. 如果获取锁失败 → 已被别人抢走，提示"已被抢走"
4. 如果获取锁成功 → 检查订单状态是否为 PAID
   - 是 → 更新订单 employee_id = 当前员工, status = IN_PROGRESS, 释放锁
   - 否 → 释放锁，提示"订单状态已变更"
5. 使用数据库乐观锁 UPDATE ... WHERE id=? AND status=1
```

### 8.2 支付流程

```
1. 用户下单 → 订单状态 UNPAID
2. 后端返回管理员微信二维码URL + 订单信息
3. 前端弹窗展示二维码，提示用户扫码支付
4. 用户扫码加微信 → 转账
5. 管理员收到转账 → 打开管理端 → 找到该订单 → 点"确认已支付"
6. 后端更新订单状态为 PAID
7. 通过 WebSocket 推送通知给所有在线员工端
```

### 8.3 WebSocket 通知机制

```
1. 员工登录后，建立 WebSocket 连接
2. 当有订单变为 PAID 状态时，服务端广播消息：
   { "type": "NEW_ORDER", "orderId": 123, "building": "A栋", ... }
3. 员工端收到消息后，可刷新待抢单列表或显示提示
4. 连接断开时，30秒内重连
```

---

## 9. 目录结构

```
xiyiji/
├── backend/                          # Spring Boot 后端
│   ├── src/main/java/com/xiyiji/
│   │   ├── common/                    # 公共模块
│   │   │   ├── config/                # 配置类（WebSocket、Redis等）
│   │   │   ├── constant/              # 常量定义
│   │   │   ├── exception/             # 全局异常处理
│   │   │   └── result/                # 统一返回结果
│   │   ├── modules/
│   │   │   ├── user/                  # 用户端
│   │   │   │   ├── controller/
│   │   │   │   ├── service/
│   │   │   │   └── entity/
│   │   │   ├── admin/                 # 管理端
│   │   │   │   ├── controller/
│   │   │   │   ├── service/
│   │   │   │   └── entity/
│   │   │   ├── employee/              # 员工端
│   │   │   │   ├── controller/
│   │   │   │   ├── service/
│   │   │   │   └── entity/
│   │   │   └── order/                 # 订单公共逻辑
│   │   │       ├── entity/
│   │   │       ├── mapper/
│   │   │       └── service/
│   │   └── XiyijiApplication.java
│   └── src/main/resources/
│       ├── application.yml
│       └── mapper/
│
├── frontend/                         # 前端 H5 项目（Vue3 + Vant）
│   ├── src/
│   │   ├── views/
│   │   │   ├── user/                 # 用户端页面
│   │   │   ├── admin/                # 管理端页面
│   │   │   └── employee/             # 员工端页面
│   │   ├── router/
│   │   ├── store/
│   │   ├── utils/
│   │   └── App.vue
│   ├── index.html
│   ├── vite.config.ts
│   └── package.json
│
└── 设计文档.md
```

---

## 10. 环境要求

| 工具 | 版本 |
|------|------|
| JDK | 1.8+ |
| Maven | 3.6+ |
| Node.js | 16+ |
| MySQL | 8.0+ |
| Redis | 6.0+ |

---

## 11. 后续可扩展

- **微信小程序**：H5 验证通过后，可打包成小程序
- **微信支付集成**：后续如需合规线上支付，可接入微信支付
- **评价系统**：用户服务完成后可评价
- **消息通知**：接入短信或微信模板消息通知