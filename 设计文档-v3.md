# 学校洗衣机清洗服务系统 - 设计文档 v3

> 基于手机号统一登录，用户可申请成为员工，管理员审核。新增数据隔离、操作日志、订单状态可视化展示。

## 1. 项目概述

学校洗衣机清洗服务系统，为在校学生提供洗衣机上门清洗服务。系统基于**手机号统一登录**，所有人注册后默认为**普通用户**，可申请成为员工并由管理员审核。

### 核心业务逻辑

手机号登录 → 下单 → 填写楼栋/房间号/上门时间 → 生成未支付订单 → 展示管理员微信二维码 → 用户扫码加微信转账 → 管理员确认已支付 → **订单同步到员工端** → 员工抢单 → 上门服务 → 完成 → **操作日志记录**

### 角色与权限

| 角色 | 获取方式 | 权限 |
|------|----------|------|
| **用户** | 手机号注册即成为用户 | 下单、**查看自己的订单**（数据隔离）、申请成为员工 |
| **员工** | 用户申请 + 管理员审核通过 | 抢单、服务、查看已接订单 |
| **管理员** | 系统预设，无法注册 | 管理订单、审核员工申请、管理员工、配置服务时间、**查看操作日志** |

---

## 2. 技术栈

| 层次 | 技术 | 说明 |
|------|------|------|
| 后端框架 | Spring Boot 3.2+ | Java 后端 |
| ORM | MyBatis-Plus 3.5+ | 数据库操作 |
| 数据库 | MySQL 8.0 | 数据存储 |
| 缓存 | Redis | 抢单锁、会话管理 |
| 实时通信 | WebSocket | 员工端订单通知 |
| 前端框架 | Vue 3 + Vant 4 | H5移动端UI |
| 构建工具 | Vite | 前端打包 |

---

## 3. 系统架构图

```
┌──────────────────────────────────────────────────────────────────────────┐
│                    前端 H5（Vue3 + Vant）                                │
│  ┌──────────────┐  ┌───────────┐  ┌───────────┐                        │
│  │   统一登录页   │  │ 用户端     │  │ 员工端     │                        │
│  │ (手机号+管理入口)│  │ /user/*   │  │ /emp/*    │                        │
│  └──────┬───────┘  └─────┬─────┘  └─────┬─────┘                        │
│         │                │              │                              │
│  ┌─────────────────────────────────────────────────────────┐            │
│  │              管理端 /admin/*                             │            │
│  └─────────────────────┬───────────────────────────────────┘            │
└────────────────────────┼─────────────────────────────────────────────────┘
                         │
┌────────────────────────▼─────────────────────────────────────────────────┐
│               后端 API（Spring Boot）                                   │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌───────────────┐ ┌──────────┐   │
│  │ Auth │ │ 用户  │ │ 管理  │ │ 员工  │ │ 订单+系统     │ │ 日志记录  │   │
│  │ Ctl  │ │ Ctl   │ │ Ctl   │ │ Ctl   │ │ Ctl           │ │ Service  │   │
│  └──────┘ └──────┘ └──────┘ └──────┘ └───────────────┘ └──────────┘   │
│  ┌────────────────────────────────────────────────────────────────────┐  │
│  │ Service • Mapper • Entity                                         │  │
│  └────────────────────────────────────────────────────────────────────┘  │
└────────────────────────┬─────────────────────────────────────────────────┘
                         │
              ┌──────────┴───────────┐
              ▼                      ▼
        ┌──────────┐          ┌──────────┐
        │  MySQL   │          │  Redis   │
        └──────────┘          └──────────┘
```

---

## 4. 业务流程

### 4.1 用户登录/注册流程

```
打开H5 → 输入手机号 → 点击登录
    ↓
系统查询手机号是否存在
    ├── 存在 → 直接登录（手机号为唯一标识）
    └── 不存在 → 自动注册为新用户 → 登录
            ↓
    进入用户端首页
    ↓
    如果该手机号关联了已审核通过的员工身份
    → 底部导航栏出现"员工"入口

管理员入口：
登录首页 → 点击"管理员登录"按钮 → 跳转到 /admin/login
```

### 4.2 用户下单支付流程（数据同步）

```
用户在首页点击"预约清洗"
    ↓
    ├── 填写楼栋（分类选择）
    ├── 填写房间号
    ├── 选择服务日期
    └── 选择时间段（起始~终止时间）
    ↓
提交 → 创建订单（状态：UNPAID）
    ↓
**订单信息同步到数据库 + 管理端订单列表实时更新**
    ↓
弹出管理员微信二维码 → 用户扫码加微信转账
    ↓
管理员在管理端确认已支付 → 订单状态变为 PAID
    ↓
**订单状态同步到员工端（WebSocket推送）**
```

### 4.3 员工接单服务流程

```
订单状态变为 PAID
    ↓
WebSocket 推送通知 → 所有在线员工端收到提醒
    ↓
员工查看可抢订单列表 → 点击抢单（Redis锁防并发）
    ↓
抢单成功 → 订单绑定该员工 → 状态 IN_PROGRESS
    ↓
员工上门清洗 → 点击"完成服务" → 状态 COMPLETED
```

### 4.4 员工申请与审核流程

```
用户在"我的"页面 → 点击"申请成为员工"
    ↓
填写姓名，电话号码和学院专业 → 提交申请
    ↓
申请记录写入 employee_application 表，status = PENDING
    ↓
管理员在管理端"员工审核"页面看到待审核列表
    ↓
管理员审核
    ├── 通过 → employee_application.status = APPROVED
    │         → employee 表新增记录，关联 user_id
    │         → 用户下次登录自动获得员工权限
    └── 拒绝 → employee_application.status = REJECTED
              → 用户可查看拒绝原因并重新申请
```

### 4.5 操作日志流程

```
订单状态变更/操作发生
    ↓
自动记录到 order_status_log 表
    ├── order_id: 订单ID
    ├── from_status: 原状态
    ├── to_status: 新状态  
    ├── operator_type: 操作者类型（0-用户, 1-管理员, 2-员工）
    ├── operator_id: 操作者ID
    └── create_time: 操作时间
    ↓
管理员可在订单详情页查看完整操作日志
```

---

## 5. 数据库设计

### 5.1 用户表（user）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键ID |
| phone | VARCHAR(20) | 手机号（唯一标识） |
| nickname | VARCHAR(50) | 昵称 |
| avatar | VARCHAR(255) | 头像URL |
| role | TINYINT | 角色：0-普通用户, 1-员工（同时是员工） |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

> 以手机号为唯一登录凭证，无密码设计。

### 5.2 管理员表（admin）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键ID |
| username | VARCHAR(50) | 登录账号 |
| password | VARCHAR(255) | 登录密码 |
| name | VARCHAR(50) | 管理员姓名 |
| wechat_qrcode | VARCHAR(255) | 微信收款二维码图片URL |
| create_time | DATETIME | 创建时间 |

### 5.3 员工表（employee）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键ID |
| user_id | BIGINT | 关联用户ID（外键→user表） |
| name | VARCHAR(50) | 员工姓名 |
| phone | VARCHAR(20) | 手机号 |
| status | TINYINT | 工作状态：0-空闲, 1-服务中 |
| is_active | TINYINT | 是否启用：0-禁用, 1-启用 |
| create_time | DATETIME | 创建时间 |

### 5.4 员工申请表（employee_application）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键ID |
| user_id | BIGINT | 申请人用户ID |
| name | VARCHAR(50) | 申请人姓名 |
| phone | VARCHAR(20) | 申请人手机号 |
| status | TINYINT | 状态：0-待审核, 1-已通过, 2-已拒绝 |
| remark | VARCHAR(255) | 审核备注/拒绝原因 |
| handler_id | BIGINT | 审核人（管理员ID） |
| handle_time | DATETIME | 审核时间 |
| create_time | DATETIME | 申请时间 |

### 5.5 订单表（orders）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键ID |
| order_no | VARCHAR(32) | 订单编号 |
| user_id | BIGINT | 用户ID（下单人）→ **数据隔离依据** |
| building_category | VARCHAR(20) | 楼栋分类：食宿楼/学生宿舍/教师公寓 |
| building | VARCHAR(50) | 楼栋号 |
| room_no | VARCHAR(50) | 房间号 |
| service_date | DATE | 服务日期 |
| time_start | VARCHAR(10) | 服务起始时间（如 10:00） |
| time_end | VARCHAR(10) | 服务结束时间（如 12:00） |
| status | TINYINT | 状态：0-未支付, 1-已支付待服务, 2-服务中, 3-已完成, 4-已取消 |
| employee_id | BIGINT | 接单员工ID（可为空） |
| remark | VARCHAR(255) | 备注 |
| create_time | DATETIME | 创建时间 |
| pay_time | DATETIME | 支付确认时间 |
| complete_time | DATETIME | 完成时间 |

### 5.6 订单状态日志表（order_status_log）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键ID |
| order_id | BIGINT | 订单ID |
| from_status | TINYINT | 原状态 |
| to_status | TINYINT | 新状态 |
| operator_type | TINYINT | 操作者类型：0-用户, 1-管理员, 2-员工 |
| operator_id | BIGINT | 操作者ID |
| create_time | DATETIME | 创建时间 |

> **操作日志记录**：记录订单的每一次状态变更，便于管理员追踪订单操作历史

### 5.7 服务时间配置表（service_time_config）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| id | BIGINT | 主键ID |
| period_name | VARCHAR(20) | 时段名称：上午/下午/晚上 |
| start_time | VARCHAR(10) | 起始时间（如 08:00） |
| end_time | VARCHAR(10) | 结束时间（如 12:00） |
| enabled | TINYINT | 是否启用：0-禁用, 1-启用 |
| create_time | DATETIME | 创建时间 |
| update_time | DATETIME | 更新时间 |

---

## 6. 订单状态机

```
                    ┌──────────┐
                    │  UNPAID  │  (0-未支付) ◄──┐
                    └────┬─────┘                │
                         │ 管理员确认支付        │
                    ┌────▼─────┐                │
                    │   PAID   │  (1-已支付待服务)│
                    └────┬─────┘                │
                         │ 员工抢单         管理员回退
                    ┌────▼────────┐             │
                    │ IN_PROGRESS │  (2-服务中)  │
                    └────┬────────┘             │
                         │ 员工完成服务          │
                    ┌────▼──────┐               │
                    │ COMPLETED │  (3-已完成)    │
                    └───────────┘               │
                                                │
  任何状态下可取消 ──→ CANCELLED (4-已取消)

       PAID ────────────────────────────────────┘
       （管理员可回退到 UNPAID，防止误触）
```

### 状态可视化展示规范

| 状态 | 样式 | 说明 |
|------|------|------|
| 已完成 | 绿色圆圈 + 绿色线条 | 表示该状态已完成 |
| 当前状态 | 蓝色圆圈 + 蓝色边框 | 表示当前所处状态 |
| 未到达 | 浅灰色圆圈 + 浅灰色线条 | 表示该状态尚未到达 |

---

## 7. API 接口设计

### 7.1 通用接口

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | /api/auth/login | 手机号登录/注册 | 无需登录 |
| GET | /api/auth/user-info | 获取当前用户信息 | 登录后 |
| GET | /api/user/time-slots | 获取可用服务时间段 | 无需登录 |

### 7.2 用户端接口（数据隔离）

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | /api/user/order/create | 创建订单 | 用户 |
| GET | /api/user/order/list | 我的订单列表 **(仅返回当前用户的订单)** | 用户 |
| GET | /api/user/order/detail/{id} | 订单详情 **(仅允许查看自己的订单)** | 用户 |
| POST | /api/user/order/cancel/{id} | 取消订单 | 用户 |
| GET | /api/user/admin/qrcode | 获取管理员微信二维码 | 用户 |
| POST | /api/user/apply-employee | 申请成为员工 | 用户 |
| GET | /api/user/apply-status | 查看申请状态 | 用户 |

### 7.3 管理端接口

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| POST | /api/admin/login | 管理员登录 | 无需登录 |
| GET | /api/admin/order/list | 订单列表（可筛选状态） | 管理员 |
| GET | /api/admin/order/detail/{id} | 订单详情 + 操作日志 | 管理员 |
| POST | /api/admin/order/confirm-pay/{id} | 确认已支付（带确认弹窗） | 管理员 |
| POST | /api/admin/order/revert-pay/{id} | 回退支付状态 PAID→UNPAID（防误触） | 管理员 |
| GET | /api/admin/order/logs/{orderId} | 获取订单操作日志 | 管理员 |
| GET | /api/admin/employee/list | 员工列表 | 管理员 |
| POST | /api/admin/employee/remove/{id} | 删除员工 | 管理员 |
| GET | /api/admin/employee/applications | 待审核员工申请列表 | 管理员 |
| POST | /api/admin/employee/approve/{id} | 通过员工申请 | 管理员 |
| POST | /api/admin/employee/reject/{id} | 拒绝员工申请 | 管理员 |
| GET | /api/admin/time-config/list | 获取服务时间配置 | 管理员 |
| PUT | /api/admin/time-config/update | 更新服务时间配置 | 管理员 |

### 7.4 员工端接口

| 方法 | 路径 | 说明 | 权限 |
|------|------|------|------|
| GET | /api/employee/orders/available | 可抢单列表 | 员工 |
| POST | /api/employee/order/grab/{id} | 抢单 | 员工 |
| GET | /api/employee/orders/my-list | 我的订单 | 员工 |
| POST | /api/employee/order/start/{id} | 开始服务 | 员工 |
| POST | /api/employee/order/complete/{id} | 完成服务 | 员工 |

### 7.5 WebSocket 接口

| 端点 | 说明 |
|------|------|
| ws://host/ws/employee/{token} | 员工端WebSocket连接，新订单推送 |

---

## 8. 前端页面设计

### 8.1 通用页面

| 页面 | 路由 | 说明 |
|------|------|------|
| 登录页 | /login | 手机号输入 + 登录/注册 + **管理员登录入口按钮** |

### 8.2 用户端页面

| 页面 | 路由 | 说明 |
|------|------|------|
| 首页 | /user/home | 服务介绍、下单入口、服务流程展示、**顶部/底部预约按钮均支持跳转** |
| 下单页 | /user/order/create | 表单：楼栋分类+楼栋号+房间号+日期+时间段+备注 |
| 支付页 | /user/order/pay | 订单摘要 + 管理员微信二维码 |
| 订单列表 | /user/order/list | 我的订单，按状态分组展示 **(数据隔离：仅显示自己的订单)** |
| 订单详情 | /user/order/detail/{id} | **时间轴进度追踪（状态可视化：已完成绿色、当前蓝色、未到浅灰色）** + 订单信息 + 操作日志 |
| 个人中心 | /user/profile | 个人信息 + "申请成为员工"入口 |

### 8.3 管理端页面

| 页面 | 路由 | 说明 |
|------|------|------|
| 登录页 | /admin/login | 账号密码登录 |
| 订单管理 | /admin/order/list | 全部订单，按状态Tab筛选，确认支付按钮 |
| 订单详情 | /admin/order/detail/{id} | 订单完整信息 + **操作日志列表** |
| 员工管理 | /admin/employee/list | 已启用员工列表，删除员工 |
| 员工审核 | /admin/employee/audit | 待审核的员工申请列表，通过/拒绝操作 |
| 服务时间配置 | /admin/time-config | 配置上午/下午/晚上的服务时间段 |
| 数据统计 | /admin/statistics | 订单量、收入概览 |

### 8.4 员工端页面

| 页面 | 路由 | 说明 |
|------|------|------|
| 待抢单 | /employee/available | 可抢订单列表，实时刷新 |
| 我的订单 | /employee/my-orders | 已接订单（服务中/已完成） |
| 订单详情 | /employee/order/detail/{id} | 订单信息 + 开始/完成操作 |

---

## 9. 前端导航结构

```
App 底部导航栏（根据角色动态显示）：

用户角色（手机号登录后）：
  ┌──────┐  ┌──────┐  ┌──────┐
  │ 首页  │  │ 订单  │  │ 我的  │
  └──────┘  └──────┘  └──────┘

用户+员工角色（申请被批准后自动增加）：
  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐
  │ 首页  │  │ 订单  │  │ 员工  │  │ 我的  │
  └──────┘  └──────┘  └──────┘  └──────┘

管理员（独立登录，独立导航）：
  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐
  │ 订单  │  │ 员工  │  │ 审核  │  │ 配置  │
  └──────┘  └──────┘  └──────┘  └──────┘
```

---

## 10. 关键功能详细设计

### 10.1 手机号登录/注册（含管理员入口）

```
1. 用户输入手机号 → 点击"登录/注册"
2. 后端查询 user 表
   - 存在 → 生成 token，返回用户信息+角色
   - 不存在 → 插入新记录（role=0），生成 token
3. 前端存储 token + 用户信息到 localStorage
4. 跳转用户端首页 /user/home
5. 后续请求在 header 中携带 token: "Authorization: Bearer {token}"
6. 如果用户同时是员工（role=1），底部导航出现"员工"入口

管理员入口：
7. 登录首页显示"管理员登录"按钮
8. 点击后跳转到 /admin/login 页面
```

### 10.2 数据隔离机制

```
用户端订单查询：
1. 用户请求 /api/user/order/list
2. 后端拦截器解析 token 获取 userId
3. SQL 查询条件自动添加：WHERE user_id = #{userId}
4. 返回该用户的订单列表，其他用户订单不可见

订单详情访问控制：
1. 用户请求 /api/user/order/detail/{id}
2. 后端先查询订单的 user_id
3. 对比当前登录用户的 userId
4. 如果不一致 → 返回 403 无权限
5. 如果一致 → 返回订单详情
```

### 10.3 操作日志记录

```
订单状态变更时：
1. 触发状态变更的 Service 方法
2. 在更新订单状态前/后，调用日志记录服务
3. 记录内容：
   - order_id: 订单ID
   - from_status: 变更前状态
   - to_status: 变更后状态
   - operator_type: 操作者类型（0-用户, 1-管理员, 2-员工）
   - operator_id: 当前操作者ID
   - create_time: 当前时间

日志查询：
1. 管理员查看订单详情时
2. 自动查询该订单的所有操作日志
3. 按时间倒序展示，便于追踪操作历史
```

### 10.4 订单状态可视化展示

```
时间轴组件设计：
1. 订单详情页展示状态时间轴
2. 四个状态节点：未支付 → 已支付 → 服务中 → 已完成
3. 样式规则：
   - 已完成状态：绿色圆圈 + 绿色连接线
   - 当前状态：蓝色圆圈（加粗边框）+ 蓝色连接线
   - 未到达状态：浅灰色圆圈 + 浅灰色连接线
4. 每个状态节点显示状态名称和时间
```

### 10.5 抢单逻辑（防并发 + 确认弹窗）

```
前端：
1. 员工点击"抢单"
2. 弹出确认弹窗："确认抢此订单？"
   ├── 确认 → 发送抢单请求
   └── 取消 → 关闭弹窗，不做操作

后端：
1. 收到抢单请求
2. 用 Redis 分布式锁：SETNX order:lock:{orderId} {employeeId}
3. 如果获取锁失败 → 已被别人抢走，提示"已被抢走"
4. 如果获取锁成功 → 检查订单状态是否为 PAID
   - 是 → 更新订单 employee_id = 当前员工, status = IN_PROGRESS
   - 否 → 释放锁，提示"订单状态已变更"
5. 使用数据库乐观锁 UPDATE ... WHERE id=? AND status=1
6. 抢单成功后广播通知其他员工该订单已被抢
```

### 10.6 支付流程（支持回退）

```
1. 用户下单 → 订单状态 UNPAID
2. 后端返回管理员微信二维码URL + 订单信息
3. 前端弹窗展示二维码，提示用户扫码支付
4. 用户扫码加微信 → 转账给管理员

管理员确认支付（带确认弹窗）：
5. 管理员在管理端 → 订单列表找到该订单 → 点击"确认已支付"
6. 弹出确认弹窗：「确认已收到用户 xxx 的支付？此操作可回退」
   ├── 确认 → 订单状态变为 PAID，通过 WebSocket 通知员工端
   └── 取消 → 不做操作

管理员回退支付（防误触）：
7. 如果管理员误操作确认了支付，在员工抢单前可回退
8. 管理员在订单详情页 → 点击"回退支付"
9. 弹出二次确认弹窗：「确认回退此订单支付状态？订单将回到未支付状态」
   ├── 确认 → 订单状态从 PAID 回退到 UNPAID
   └── 取消 → 不做操作
10. 已抢单的订单不可回退
```

### 10.7 员工申请与审核

```
用户端：
1. 用户在"我的"页面点击"申请成为员工"
2. 填写姓名 → 提交申请
3. 记录到 employee_application 表，status=PENDING
4. 页面显示"申请已提交，等待管理员审核"

管理端：
1. 管理员在"员工审核"页面看到待审核列表
2. 查看申请人信息（手机号、姓名）
3. 点击"通过" → employee_application.status=APPROVED
   → employee 表新增记录，关联 user_id
   → user.role 更新为 1（员工）
4. 点击"拒绝" → employee_application.status=REJECTED
   → 填写拒绝原因

用户收到结果：
1. 用户下次登录或刷新时，检测申请状态
2. 通过 → 获得员工权限，导航出现"员工"入口
3. 拒绝 → 显示拒绝原因，可重新申请
```

---

## 11. 目录结构

```
xiyiji/
├── backend/                          # Spring Boot 后端
│   ├── src/main/java/com/xiyiji/
│   │   ├── common/                    # 公共模块
│   │   │   ├── config/                # 配置类
│   │   │   ├── constant/              # 常量定义
│   │   │   ├── interceptor/           # Token 登录拦截器
│   │   │   ├── exception/             # 全局异常处理
│   │   │   └── result/                # 统一返回结果 R<T>
│   │   ├── modules/
│   │   │   ├── auth/                  # 登录认证模块
│   │   │   │   ├── controller/
│   │   │   │   ├── service/
│   │   │   │   └── entity/
│   │   │   ├── admin/                 # 管理端模块
│   │   │   │   ├── controller/
│   │   │   │   ├── service/
│   │   │   │   └── entity/
│   │   │   ├── employee/              # 员工端模块
│   │   │   │   ├── controller/
│   │   │   │   ├── service/
│   │   │   │   └── entity/
│   │   │   ├── order/                 # 订单模块
│   │   │   │   ├── controller/
│   │   │   │   ├── service/
│   │   │   │   ├── mapper/
│   │   │   │   └── entity/
│   │   │   └── system/                # 系统配置模块
│   │   │       ├── controller/
│   │   │       ├── service/
│   │   │       ├── mapper/
│   │   │       └── entity/
│   │   └── XiyijiApplication.java
│   └── src/main/resources/
│       ├── application.yml
│       └── sql/init.sql
│
├── frontend/                         # 前端 H5 项目
│   ├── src/
│   │   ├── views/
│   │   │   ├── LoginPage.vue         # 手机号登录页（含管理员入口）
│   │   │   ├── user/                 # 用户端页面
│   │   │   ├── admin/                # 管理端页面
│   │   │   └── employee/             # 员工端页面
│   │   ├── router/
│   │   ├── store/
│   │   ├── utils/
│   │   │   ├── request.ts
│   │   │   └── auth.ts
│   │   └── App.vue
│   ├── index.html
│   ├── vite.config.ts
│   └── package.json
│
├── 设计文档.md                        # v1 原始设计文档
├── 设计文档-v2.md                     # v2 设计文档
└── 设计文档-v3.md                     # v3 设计文档（本文件）
```

---

## 12. 完整页面路由表

```
# 通用
/login                     → 手机号登录/注册页（默认入口，含管理员登录按钮）

# 用户端（手机号登录后）
/user/home                 → 首页（顶部/底部预约按钮支持跳转）
/user/order/create         → 下单页
/user/order/pay            → 支付页
/user/order/list           → 我的订单（数据隔离）
/user/order/detail/:id     → 订单详情（状态可视化时间轴）
/user/profile              → 个人中心 + 申请成为员工

# 管理端（管理员账号登录）
/admin/login               → 管理员登录
/admin/order/list          → 订单管理
/admin/order/detail/:id    → 订单详情（含操作日志）
/admin/employee/list       → 员工列表
/admin/employee/audit      → 员工申请审核
/admin/time-config         → 服务时间配置
/admin/statistics          → 数据统计

# 员工端（需员工身份）
/employee/available        → 待抢单
/employee/my-orders        → 我的订单
/employee/order/detail/:id → 订单详情
```

---

## 13. 环境要求

| 工具 | 版本 |
|------|------|
| JDK | 17+ |
| Maven | 3.6+ |
| Node.js | 18+ |
| MySQL | 8.0+ |
| Redis | 6.0+ |

---

## 14. 新增功能清单（v3 版本）

| 功能 | 说明 | 状态 |
|------|------|------|
| 管理员登录入口 | 登录首页新增"管理员登录"按钮 | 待实现 |
| 数据隔离 | 用户只能查看自己的订单 | 待实现 |
| 操作日志 | 记录订单状态变更，管理员可查看 | 待实现 |
| 订单状态可视化 | 时间轴展示：已完成绿色、当前蓝色、未到浅灰色 | 待实现 |
| 预约按钮跳转 | 首页顶部和底部预约按钮均支持跳转 | 待实现 |
| 订单同步机制 | 用户下单同步到数据库和管理端，管理员确认支付同步到员工端 | 待实现 |

---

## 15. 后续可扩展

- **微信小程序**：H5 验证通过后，可打包成小程序
- **微信支付集成**：后续如需合规线上支付，可接入微信支付
- **评价系统**：用户服务完成后可评价
- **短信验证码**：提升手机号登录安全性
- **消息通知**：接入公众号模板消息通知