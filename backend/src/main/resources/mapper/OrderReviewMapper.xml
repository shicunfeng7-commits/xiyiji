<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiyiji.modules.review.mapper.OrderReviewMapper">

    <select id="selectByOrderId" resultType="com.xiyiji.modules.review.entity.OrderReview">
        SELECT r.*, u.phone, u.nickname
        FROM order_review r
        LEFT JOIN user u ON r.user_id = u.id
        WHERE r.order_id = #{orderId}
    </select>

    <select id="countByOrderId" resultType="int">
        SELECT COUNT(*) FROM order_review WHERE order_id = #{orderId}
    </select>

</mapper>
